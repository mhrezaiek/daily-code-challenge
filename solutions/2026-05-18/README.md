# Daily Code Challenge — 2026-05-18

Solved 4 problem(s) today:

- **Subsets** (medium, backtracking) — `subsets.py`
- **Permutations** (medium, backtracking) — `permutations.py`
- **3Sum** (medium, two-pointer) — `3sum.py`
- **Reorder List** (medium, linked-list) — `reorder-list.py`

## Prompts

### Subsets

> Given an integer array nums of unique elements, return all possible subsets.

### Permutations

> Given an array nums of distinct integers, return all the possible permutations.

### 3Sum

> Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i, j, k are distinct and nums[i] + nums[j] + nums[k] == 0.

### Reorder List

> You are given the head of a singly linked-list. Reorder the list to: L0 → Ln → L1 → Ln-1 → L2 → Ln-2 → …
