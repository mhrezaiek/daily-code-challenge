# Daily Code Challenge — 2026-05-29

Solved 4 problem(s) today:

- **Valid Anagram** (easy, hashmap-string) — `valid-anagram.py`
- **Kth Largest Element in an Array** (medium, heap) — `kth-largest.py`
- **Maximum Depth of Binary Tree** (easy, tree) — `max-depth-bt.py`
- **Sliding Window Maximum** (hard, deque) — `sliding-window-max.py`

## Prompts

### Valid Anagram

> Given two strings s and t, return true if t is an anagram of s.

### Kth Largest Element in an Array

> Given an integer array nums and an integer k, return the kth largest element in the array.

### Maximum Depth of Binary Tree

> Given the root of a binary tree, return its maximum depth.

### Sliding Window Maximum

> Given an array nums and a sliding window of size k, return the max element in each window.
