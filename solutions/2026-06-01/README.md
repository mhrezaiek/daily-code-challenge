# Daily Code Challenge — 2026-06-01

Solved 2 problem(s) today:

- **Decode Ways** (medium, dp-string) — `decode-ways.py`
- **Partition Equal Subset Sum** (medium, dp-knapsack) — `partition-equal-subset.py`

## Prompts

### Decode Ways

> A message containing letters from A-Z can be encoded into numbers (A=1, ..., Z=26). Given a string s containing only digits, return the number of ways to decode it.

### Partition Equal Subset Sum

> Given a non-empty array nums containing only positive integers, find if the array can be partitioned into two subsets such that the sum of elements in both subsets is equal.
