# Daily Code Challenge — 2026-05-20

Solved 4 problem(s) today:

- **Kth Smallest Element in a BST** (medium, tree) — `kth-smallest-bst.py`
- **Validate Binary Search Tree** (medium, tree) — `validate-bst.py`
- **Min Stack** (easy, design-stack) — `min-stack.py`
- **Longest Consecutive Sequence** (medium, hashset) — `longest-consecutive.py`

## Prompts

### Kth Smallest Element in a BST

> Given the root of a binary search tree, and an integer k, return the kth smallest value in the tree.

### Validate Binary Search Tree

> Given the root of a binary tree, determine if it is a valid binary search tree.

### Min Stack

> Design a stack that supports push, pop, top, and retrieving the minimum element in constant time.

### Longest Consecutive Sequence

> Given an unsorted array of integers nums, return the length of the longest consecutive elements sequence in O(n).
